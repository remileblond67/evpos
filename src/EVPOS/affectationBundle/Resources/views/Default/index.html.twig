{% extends "EVPOSaffectationBundle::layout.html.twig" %}
{% block title%}Suivi de la migration MOCA{% endblock%}

{% block content %}
<div class="row div col-lg-3"><div class="panel panel-default">
        <div class="panel-heading">
            <h3 class="panel-title">Application courante</h3>
        </div>
        <div class="panel-body">
            <p>Environnement {{ app.environment }}.</p>
            {% if app.user %}
            <p>Connecté en tant que {{ app.user }}</p>
            {% endif %}
            <a href="{{ path('logout') }}" class="btn"></a>
        </div>
    </div>
</div>
{%endblock%}
